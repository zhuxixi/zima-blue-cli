apiVersion: zima.io/v1
kind: Env
metadata:
  code: github-env
  name: GitHub Env
spec:
  # 此 env 被 claude-cr 与 kimi-cr 两个 PJob 共用
  secrets:
    - name: GITHUB_TOKEN
      source: cmd
      command: gh auth token
