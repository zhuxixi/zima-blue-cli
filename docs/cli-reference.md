<!--
This file is generated by scripts/generate_cli_docs.py.
Do not edit manually.

Regenerate with:
uv run python scripts/generate_cli_docs.py
-->

# Zima Blue CLI Reference

This reference is generated from the live Typer command tree.
Command paths and parameter syntax come from the installed CLI; English descriptions are maintained in `docs/cli-descriptions.yaml`.

Use `zima --help` or `zima <command> --help` for runtime help.

## `zima`

Zima Blue CLI - personal agent orchestration platform runner.

**Usage**:

```text
zima [OPTIONS] COMMAND [ARGS]...
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `version` | `--version` | BOOL | no | — | — | no | yes |

## `zima agent`

Agent management commands

**Usage**:

```text
zima agent COMMAND [ARGS]...
```

## `zima agent create`

Create a new agent

**Usage**:

```text
zima agent create [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `example` | `--example` | BOOL | no | `false` | — | no | yes |
| `name` | `--name, -n` | TEXT | no | — | — | no | no |
| `code` | `--code, -c` | TEXT | no | — | — | no | no |
| `agent_type` | `--type, -t` | TEXT | no | `kimi` | — | no | no |
| `description` | `--description, -d` | TEXT | no | `` | — | no | no |
| `from_code` | `--from` | TEXT | no | — | — | no | no |
| `model` | `--model, -m` | TEXT | no | — | — | no | no |
| `work_dir` | `--work-dir, -w` | TEXT | no | — | — | no | no |
| `force` | `--force, -f` | BOOL | no | `false` | — | no | yes |

## `zima agent delete`

Delete an agent

**Usage**:

```text
zima agent delete [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `force` | `--force, -f` | BOOL | no | `false` | — | no | yes |

## `zima agent list`

List all agents

**Usage**:

```text
zima agent list [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `agent_type` | `--type, -t` | TEXT | no | — | — | no | no |
| `output_format` | `--format` | TEXT | no | `table` | — | no | no |

## `zima agent show`

Show agent details

**Usage**:

```text
zima agent show [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `output_format` | `--format` | TEXT | no | `yaml` | — | no | no |

## `zima agent test`

Preview the CLI command an agent would run, without executing it

**Usage**:

```text
zima agent test [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `workflow` | `--workflow, -w` | TEXT | no | — | — | no | no |
| `variable` | `--variable, -v` | TEXT | no | — | — | no | no |
| `env` | `--env, -e` | TEXT | no | — | — | no | no |
| `pmg` | `--pmg, -p` | TEXT | no | — | — | no | no |

## `zima agent types`

List all supported agent types

**Usage**:

```text
zima agent types
```

## `zima agent update`

Update agent configuration

**Usage**:

```text
zima agent update [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `name` | `--name, -n` | TEXT | no | — | — | no | no |
| `description` | `--description, -d` | TEXT | no | — | — | no | no |
| `set_param` | `--set-param, -p` | TEXT | no | — | — | yes | no |

## `zima agent validate`

Validate agent configuration

**Usage**:

```text
zima agent validate CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |

## `zima daemon`

Daemon management commands

**Usage**:

```text
zima daemon COMMAND [ARGS]...
```

## `zima daemon logs`

Show daemon logs

**Usage**:

```text
zima daemon logs [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `tail` | `--tail, -n` | INTEGER | no | `20` | — | no | no |

## `zima daemon start`

Start the global daemon

**Usage**:

```text
zima daemon start [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `schedule` | `--schedule, -s` | TEXT | yes | — | — | no | no |

## `zima daemon status`

Show daemon status

**Usage**:

```text
zima daemon status
```

## `zima daemon stop`

Stop the global daemon

**Usage**:

```text
zima daemon stop
```

## `zima env`

Environment configuration management commands

**Usage**:

```text
zima env COMMAND [ARGS]...
```

## `zima env create`

Create a new environment configuration

**Usage**:

```text
zima env create [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `example` | `--example` | BOOL | no | `false` | — | no | yes |
| `code` | `--code, -c` | TEXT | no | — | — | no | no |
| `name` | `--name, -n` | TEXT | no | — | — | no | no |
| `for_type` | `--for-type, -t` | TEXT | no | — | — | no | no |
| `description` | `--description, -d` | TEXT | no | `` | — | no | no |
| `from_code` | `--from` | TEXT | no | — | — | no | no |
| `force` | `--force, -f` | BOOL | no | `false` | — | no | yes |

## `zima env delete`

Delete an environment configuration

**Usage**:

```text
zima env delete [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `force` | `--force, -f` | BOOL | no | `false` | — | no | yes |

## `zima env export`

Export environment configuration as dotenv, shell, or JSON

**Usage**:

```text
zima env export [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `format` | `--format, -f` | TEXT | no | `dotenv` | — | no | no |
| `output` | `--output, -o` | TEXT | no | — | — | no | no |
| `resolve_secrets` | `--resolve-secrets` | BOOL | no | `false` | — | no | yes |

## `zima env get`

Get an environment variable value

**Usage**:

```text
zima env get [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `key` | `--key, -k` | TEXT | yes | — | — | no | no |
| `resolve` | `--resolve` | BOOL | no | `false` | — | no | yes |

## `zima env list`

List all environment configurations

**Usage**:

```text
zima env list [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `for_type` | `--for-type, -t` | TEXT | no | — | — | no | no |
| `output_format` | `--format` | TEXT | no | `table` | — | no | no |

## `zima env set`

Set an environment variable or secret reference

**Usage**:

```text
zima env set [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `key` | `--key, -k` | TEXT | yes | — | — | no | no |
| `value` | `--value, -v` | TEXT | no | — | — | no | no |
| `secret` | `--secret, -s` | BOOL | no | `false` | — | no | yes |
| `source` | `--source` | TEXT | no | `env` | — | no | no |
| `source_key` | `--source-key` | TEXT | no | — | — | no | no |
| `source_path` | `--source-path` | TEXT | no | — | — | no | no |
| `source_cmd` | `--source-cmd` | TEXT | no | — | — | no | no |

## `zima env show`

Show environment configuration details

**Usage**:

```text
zima env show [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `output_format` | `--format` | TEXT | no | `yaml` | — | no | no |
| `resolve_secrets` | `--resolve-secrets` | BOOL | no | `false` | — | no | yes |

## `zima env unset`

Remove an environment variable

**Usage**:

```text
zima env unset [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `key` | `--key, -k` | TEXT | yes | — | — | no | no |

## `zima env update`

Update environment configuration

**Usage**:

```text
zima env update [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `name` | `--name, -n` | TEXT | no | — | — | no | no |
| `description` | `--description, -d` | TEXT | no | — | — | no | no |
| `override_existing` | `--override-existing` | BOOL | no | — | — | no | yes |

## `zima env validate`

Validate environment configuration

**Usage**:

```text
zima env validate CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |

## `zima pjob`

PJob management - execute Agent tasks

**Usage**:

```text
zima pjob COMMAND [ARGS]...
```

## `zima pjob actions`

Manage PJob postExec actions and provider

**Usage**:

```text
zima pjob actions CODE COMMAND [ARGS]...
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |

## `zima pjob actions add`

Add a postExec action to a PJob

**Usage**:

```text
zima pjob actions add [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `condition` | `--condition` | TEXT | yes | — | — | no | no |
| `action_type` | `--type` | TEXT | yes | — | — | no | no |
| `add_label` | `--add-label` | TEXT | no | — | — | yes | no |
| `remove_label` | `--remove-label` | TEXT | no | — | — | yes | no |
| `repo` | `--repo` | TEXT | no | `` | — | no | no |
| `issue` | `--issue` | TEXT | no | `` | — | no | no |
| `body` | `--body` | TEXT | no | `` | — | no | no |

## `zima pjob actions list`

List all postExec actions for a PJob

**Usage**:

```text
zima pjob actions list
```

## `zima pjob actions provider`

Get or set the action provider for a PJob

**Usage**:

```text
zima pjob actions provider [NAME]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `name` | `NAME` | TEXT | no | — | — | no | no |

## `zima pjob actions remove`

Remove a postExec action by index

**Usage**:

```text
zima pjob actions remove [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `index` | `--index` | INTEGER | yes | — | — | no | no |

## `zima pjob cancel`

Cancel running PJob(s)

**Usage**:

```text
zima pjob cancel [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `execution_id` | `--id` | TEXT | no | — | — | no | no |

## `zima pjob copy`

Copy a PJob

**Usage**:

```text
zima pjob copy [OPTIONS] SOURCE TARGET
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `source` | `SOURCE` | TEXT | yes | — | — | no | no |
| `target` | `TARGET` | TEXT | yes | — | — | no | no |
| `name` | `--name, -n` | TEXT | yes | — | — | no | no |

## `zima pjob create`

Create a new PJob

**Usage**:

```text
zima pjob create [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `example` | `--example` | BOOL | no | `false` | — | no | yes |
| `name` | `--name, -n` | TEXT | no | — | — | no | no |
| `code` | `--code, -c` | TEXT | no | — | — | no | no |
| `agent` | `--agent, -a` | TEXT | no | — | — | no | no |
| `workflow` | `--workflow, -w` | TEXT | no | — | — | no | no |
| `variable` | `--variable, -v` | TEXT | no | — | — | no | no |
| `env` | `--env, -e` | TEXT | no | — | — | no | no |
| `pmg` | `--pmg, -p` | TEXT | no | — | — | no | no |
| `description` | `--description, -d` | TEXT | no | `` | — | no | no |
| `label` | `--label, -l` | TEXT | no | — | — | yes | no |
| `work_dir` | `--work-dir` | TEXT | no | — | — | no | no |
| `timeout` | `--timeout, -t` | INTEGER | no | `0` | — | no | no |
| `output` | `--output, -o` | TEXT | no | — | — | no | no |
| `from_code` | `--from-code` | TEXT | no | — | — | no | no |
| `force` | `--force, -f` | BOOL | no | `false` | — | no | yes |

## `zima pjob delete`

Delete a PJob

**Usage**:

```text
zima pjob delete [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `force` | `--force, -f` | BOOL | no | `false` | — | no | yes |

## `zima pjob history`

Show or clear execution history

**Usage**:

```text
zima pjob history [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `limit` | `--limit, -n` | INTEGER | no | `20` | — | no | no |
| `status` | `--status` | TEXT | no | — | — | no | no |
| `clear` | `--clear` | BOOL | no | `false` | — | no | yes |
| `detail` | `--detail` | TEXT | no | — | — | no | no |

## `zima pjob list`

List all PJobs

**Usage**:

```text
zima pjob list [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `label` | `--label, -l` | TEXT | no | — | — | yes | no |
| `agent_type` | `--agent-type` | TEXT | no | — | — | no | no |
| `output_format` | `--format` | TEXT | no | `table` | — | no | no |

## `zima pjob ps`

List all currently running PJobs

**Usage**:

```text
zima pjob ps
```

## `zima pjob render`

Render the workflow template without executing

**Usage**:

```text
zima pjob render [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `output` | `--output, -o` | TEXT | no | — | — | no | no |
| `show_command` | `--show-command` | BOOL | no | `false` | — | no | yes |
| `show_env` | `--show-env` | BOOL | no | `false` | — | no | yes |

## `zima pjob run`

Execute a PJob (runs in background by default)

**Usage**:

```text
zima pjob run [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `dry_run` | `--dry-run` | BOOL | no | `false` | — | no | yes |
| `set_var` | `--set-var` | TEXT | no | — | — | yes | no |
| `set_env` | `--set-env` | TEXT | no | — | — | yes | no |
| `set_param` | `--set-param` | TEXT | no | — | — | yes | no |
| `work_dir` | `--work-dir` | TEXT | no | — | — | no | no |
| `keep_temp` | `--keep-temp` | BOOL | no | `false` | — | no | yes |
| `dedup_off` | `--dedup-off` | BOOL | no | `false` | — | no | yes |
| `failure_guard_off` | `--failure-guard-off` | BOOL | no | `false` | — | no | yes |
| `timeout` | `--timeout, -t` | INTEGER | no | — | — | no | no |
| `skip_validation` | `--skip-validation` | BOOL | no | `false` | — | no | yes |

## `zima pjob show`

Show PJob details

**Usage**:

```text
zima pjob show [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `history` | `--history` | BOOL | no | `false` | — | no | yes |
| `limit` | `--limit` | INTEGER | no | `10` | — | no | no |
| `output_format` | `--format` | TEXT | no | `yaml` | — | no | no |

## `zima pjob status`

Show execution status for a PJob (running and recent history)

**Usage**:

```text
zima pjob status CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |

## `zima pjob update`

Update PJob configuration

**Usage**:

```text
zima pjob update [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `agent` | `--agent, -a` | TEXT | no | — | — | no | no |
| `workflow` | `--workflow, -w` | TEXT | no | — | — | no | no |
| `variable` | `--variable, -v` | TEXT | no | — | — | no | no |
| `env` | `--env, -e` | TEXT | no | — | — | no | no |
| `pmg` | `--pmg, -p` | TEXT | no | — | — | no | no |
| `timeout` | `--timeout, -t` | INTEGER | no | — | — | no | no |
| `work_dir` | `--work-dir` | TEXT | no | — | — | no | no |
| `output` | `--output, -o` | TEXT | no | — | — | no | no |
| `add_label` | `--add-label` | TEXT | no | — | — | yes | no |
| `remove_label` | `--remove-label` | TEXT | no | — | — | yes | no |

## `zima pjob validate`

Validate PJob configuration

**Usage**:

```text
zima pjob validate [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `strict` | `--strict / --no-strict` | BOOL | no | `true` | — | no | yes |
| `check_render` | `--check-render` | BOOL | no | `false` | — | no | yes |
| `check_workdir` | `--check-workdir / --no-check-workdir` | BOOL | no | `true` | — | no | yes |

## `zima pmg`

PMG (Parameters Group) management commands

**Usage**:

```text
zima pmg COMMAND [ARGS]...
```

## `zima pmg add-param`

Add a parameter to PMG

**Usage**:

```text
zima pmg add-param [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `name` | `--name` | TEXT | yes | — | — | no | no |
| `param_type` | `--type` | TEXT | yes | — | — | no | no |
| `value` | `--value` | TEXT | no | — | — | no | no |
| `values` | `--values` | TEXT | no | — | — | no | no |
| `enabled` | `--enabled / --disabled` | BOOL | no | `true` | — | no | yes |

## `zima pmg build`

Build command-line arguments from PMG

**Usage**:

```text
zima pmg build [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `output_format` | `--format, -f` | TEXT | no | `list` | — | no | no |
| `eval_conditions` | `--eval-conditions / --no-eval-conditions` | BOOL | no | `true` | — | no | yes |

## `zima pmg create`

Create a new parameters group

**Usage**:

```text
zima pmg create [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `example` | `--example` | BOOL | no | `false` | — | no | yes |
| `code` | `--code, -c` | TEXT | no | — | — | no | no |
| `name` | `--name, -n` | TEXT | no | — | — | no | no |
| `for_types` | `--for-type, -t` | TEXT | no | — | — | yes | no |
| `description` | `--description, -d` | TEXT | no | `` | — | no | no |
| `from_code` | `--from` | TEXT | no | — | — | no | no |
| `force` | `--force, -f` | BOOL | no | `false` | — | no | yes |

## `zima pmg delete`

Delete a parameters group

**Usage**:

```text
zima pmg delete [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `force` | `--force, -f` | BOOL | no | `false` | — | no | yes |

## `zima pmg list`

List all parameters groups

**Usage**:

```text
zima pmg list [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `for_type` | `--for-type, -t` | TEXT | no | — | — | no | no |
| `output_format` | `--format` | TEXT | no | `table` | — | no | no |

## `zima pmg remove-param`

Remove a parameter from PMG

**Usage**:

```text
zima pmg remove-param [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `name` | `--name` | TEXT | yes | — | — | no | no |

## `zima pmg show`

Show PMG details

**Usage**:

```text
zima pmg show [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `output_format` | `--format` | TEXT | no | `yaml` | — | no | no |

## `zima pmg update`

Update PMG configuration

**Usage**:

```text
zima pmg update [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `name` | `--name, -n` | TEXT | no | — | — | no | no |
| `description` | `--description, -d` | TEXT | no | — | — | no | no |
| `raw` | `--raw` | TEXT | no | — | — | no | no |

## `zima pmg validate`

Validate PMG configuration

**Usage**:

```text
zima pmg validate CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |

## `zima quickstart`

Interactive wizard to create a complete PJob from scratch

**Usage**:

```text
zima quickstart [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `scene` | `--scene, -s` | TEXT | no | — | — | no | no |
| `name` | `--name, -n` | TEXT | no | — | — | no | no |
| `work_dir` | `--work-dir, -w` | TEXT | no | — | — | no | no |

## `zima schedule`

Schedule management - daemon cycle configuration

**Usage**:

```text
zima schedule COMMAND [ARGS]...
```

## `zima schedule create`

Create a new Schedule

**Usage**:

```text
zima schedule create [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `example` | `--example` | BOOL | no | `false` | — | no | yes |
| `name` | `--name, -n` | TEXT | no | — | — | no | no |
| `code` | `--code, -c` | TEXT | no | — | — | no | no |
| `force` | `--force, -f` | BOOL | no | `false` | — | no | yes |

## `zima schedule delete`

Delete a Schedule

**Usage**:

```text
zima schedule delete [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `force` | `--force, -f` | BOOL | no | `false` | — | no | yes |

## `zima schedule list`

List all Schedules

**Usage**:

```text
zima schedule list
```

## `zima schedule set-mapping`

Set the cycle type at a specific cycle-mapping index (0-31)

**Usage**:

```text
zima schedule set-mapping [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `index` | `--index` | INTEGER | yes | — | — | no | no |
| `type_id` | `--type` | TEXT | yes | — | — | no | no |

## `zima schedule set-type`

Set PJobs for a cycle type and stage

**Usage**:

```text
zima schedule set-type [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `type_id` | `--typeId` | TEXT | yes | — | — | no | no |
| `stage` | `--stage` | TEXT | yes | — | — | no | no |
| `pjobs` | `--pjobs` | TEXT | yes | — | — | yes | no |

## `zima schedule show`

Show Schedule details

**Usage**:

```text
zima schedule show CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |

## `zima schedule update`

Update Schedule metadata

**Usage**:

```text
zima schedule update [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `name` | `--name, -n` | TEXT | no | — | — | no | no |
| `description` | `--description, -d` | TEXT | no | — | — | no | no |

## `zima schedule validate`

Validate a Schedule

**Usage**:

```text
zima schedule validate [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `strict` | `--strict / --no-strict` | BOOL | no | `true` | — | no | yes |

## `zima variable`

Variable management commands

**Usage**:

```text
zima variable COMMAND [ARGS]...
```

## `zima variable create`

Create a new variable configuration

**Usage**:

```text
zima variable create [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `example` | `--example` | BOOL | no | `false` | — | no | yes |
| `name` | `--name, -n` | TEXT | no | — | — | no | no |
| `code` | `--code, -c` | TEXT | no | — | — | no | no |
| `for_workflow` | `--for-workflow, -w` | TEXT | no | — | — | no | no |
| `description` | `--description, -d` | TEXT | no | `` | — | no | no |
| `from_code` | `--from` | TEXT | no | — | — | no | no |
| `force` | `--force, -f` | BOOL | no | `false` | — | no | yes |

## `zima variable delete`

Delete a variable configuration

**Usage**:

```text
zima variable delete [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `force` | `--force, -f` | BOOL | no | `false` | — | no | yes |

## `zima variable get`

Get a variable value

**Usage**:

```text
zima variable get CODE KEY
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `key` | `KEY` | TEXT | yes | — | — | no | no |

## `zima variable list`

List all variable configurations

**Usage**:

```text
zima variable list [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `for_workflow` | `--for-workflow, -w` | TEXT | no | — | — | no | no |
| `output_format` | `--format` | TEXT | no | `table` | — | no | no |

## `zima variable merge`

Merge values from another variable configuration

**Usage**:

```text
zima variable merge [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `source` | `--from` | TEXT | yes | — | — | no | no |
| `overwrite` | `--overwrite` | BOOL | no | `false` | — | no | yes |

## `zima variable set`

Set a variable value

**Usage**:

```text
zima variable set [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `key` | `--key, -k` | TEXT | yes | — | — | no | no |
| `value` | `--value, -v` | TEXT | yes | — | — | no | no |

## `zima variable show`

Show variable configuration details

**Usage**:

```text
zima variable show [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `output_format` | `--format` | TEXT | no | `yaml` | — | no | no |

## `zima variable update`

Update variable configuration

**Usage**:

```text
zima variable update [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `name` | `--name, -n` | TEXT | no | — | — | no | no |
| `description` | `--description, -d` | TEXT | no | — | — | no | no |
| `for_workflow` | `--for-workflow, -w` | TEXT | no | — | — | no | no |

## `zima variable validate`

Validate variable configuration

**Usage**:

```text
zima variable validate [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `check_workflow` | `--check-workflow` | BOOL | no | `false` | — | no | yes |

## `zima webhook-server`

Run the GitHub webhook receiver that triggers PJobs

**Usage**:

```text
zima webhook-server [OPTIONS] COMMAND [ARGS]...
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `pjob` | `--pjob` | TEXT | no | `` | — | yes | no |
| `repo` | `--repo` | TEXT | no | `` | — | yes | no |
| `smee_url` | `--smee-url` | TEXT | no | — | — | no | no |
| `port` | `--port` | INTEGER | no | `8765` | — | no | no |
| `secret` | `--secret` | TEXT | no | — | — | no | no |
| `allow_no_secret` | `--allow-no-secret` | BOOL | no | `false` | — | no | yes |
| `skip_draft` | `--skip-draft / --no-skip-draft` | BOOL | no | `true` | — | no | yes |

## `zima workflow`

Workflow management commands

**Usage**:

```text
zima workflow COMMAND [ARGS]...
```

## `zima workflow add-var`

Add a variable definition to workflow

**Usage**:

```text
zima workflow add-var [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `name` | `--name, -n` | TEXT | yes | — | — | no | no |
| `var_type` | `--type, -t` | TEXT | no | `string` | — | no | no |
| `required` | `--required / --optional` | BOOL | no | `true` | — | no | yes |
| `default` | `--default, -d` | TEXT | no | — | — | no | no |
| `description` | `--description` | TEXT | no | `` | — | no | no |

## `zima workflow check-structure`

Check workflow template structure against the 5-module framework

**Usage**:

```text
zima workflow check-structure [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `fix` | `--fix` | BOOL | no | `false` | — | no | yes |

## `zima workflow create`

Create a new workflow

**Usage**:

```text
zima workflow create [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `example` | `--example` | BOOL | no | `false` | — | no | yes |
| `name` | `--name, -n` | TEXT | no | — | — | no | no |
| `code` | `--code, -c` | TEXT | no | — | — | no | no |
| `description` | `--description, -d` | TEXT | no | `` | — | no | no |
| `template` | `--template, -t` | TEXT | no | — | — | no | no |
| `format` | `--format, -f` | TEXT | no | `jinja2` | — | no | no |
| `from_code` | `--from` | TEXT | no | — | — | no | no |
| `force` | `--force` | BOOL | no | `false` | — | no | yes |

## `zima workflow delete`

Delete a workflow

**Usage**:

```text
zima workflow delete [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `force` | `--force, -f` | BOOL | no | `false` | — | no | yes |

## `zima workflow list`

List all workflows

**Usage**:

```text
zima workflow list [OPTIONS]
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `tag` | `--tag` | TEXT | no | — | — | no | no |
| `output_format` | `--format` | TEXT | no | `table` | — | no | no |

## `zima workflow render`

Render workflow template with variables

**Usage**:

```text
zima workflow render [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `variable` | `--variable, -v` | TEXT | no | — | — | no | no |
| `var` | `--var` | TEXT | no | — | — | yes | no |
| `output_file` | `--output, -o` | TEXT | no | — | — | no | no |

## `zima workflow show`

Show workflow details

**Usage**:

```text
zima workflow show [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `output_format` | `--format` | TEXT | no | `yaml` | — | no | no |
| `show_structure` | `--structure / --no-structure` | BOOL | no | `true` | — | no | yes |

## `zima workflow update`

Update workflow configuration

**Usage**:

```text
zima workflow update [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `name` | `--name, -n` | TEXT | no | — | — | no | no |
| `description` | `--description, -d` | TEXT | no | — | — | no | no |
| `template` | `--template, -t` | TEXT | no | — | — | no | no |
| `add_tag` | `--add-tag` | TEXT | no | — | — | yes | no |
| `remove_tag` | `--remove-tag` | TEXT | no | — | — | yes | no |
| `version` | `--version, -v` | TEXT | no | — | — | no | no |

## `zima workflow validate`

Validate workflow config, Jinja2 syntax, and 5-module structure

**Usage**:

```text
zima workflow validate [OPTIONS] CODE
```

| Parameter | Syntax | Type | Required | Default | Choices | Multiple | Flag |
|---|---|---|---|---|---|---|---|
| `code` | `CODE` | TEXT | yes | — | — | no | no |
| `check_syntax` | `--check-syntax / --no-check-syntax` | BOOL | no | `true` | — | no | yes |
| `check_vars` | `--check-vars` | BOOL | no | `false` | — | no | yes |
| `check_structure` | `--check-structure / --no-check-structure` | BOOL | no | `true` | — | no | yes |
